# Changelog

## Unreleased

- Add per-user Windows Explorer context-menu integration for folders and folder backgrounds.
- Add `--install-context-menu` and `--uninstall-context-menu` CLI commands.
- Add `GitCopy: Copy Workspace Repository` to VS Code.
- Add a VS Code File Explorer empty-space context-menu entry for copying the current workspace repository.

## 0.1.0

- Initial VS Code integration for GitCopy.
- Add copy, clean-copy, and preview commands.
- Add Explorer context-menu integration.
- Bundle the Windows x64 GitCopy engine in packaged VSIX builds.
- Add destination, confirmation, reveal, and executable-path settings.
